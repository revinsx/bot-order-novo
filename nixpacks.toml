[phases.setup]
nixPkgs = ["python3", "tesseract", "curl"]

[phases.install]
cmds = [
    "curl https://bootstrap.pypa.io/get-pip.py -o get-pip.py",
    "python3 get-pip.py",
    "python3 -m pip install -r requirements.txt"
]
